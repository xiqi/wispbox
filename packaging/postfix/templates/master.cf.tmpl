# =====================================================================
# Postfix master.cf — generated by wispbox. DO NOT EDIT.
# =====================================================================
# service type  private unpriv  chroot  wakeup  maxproc command + args
smtp      inet  n       -       y       -       4       smtpd

# Authenticated submission (STARTTLS required). This is where mail users
# and the wispbox webmail send from.
submission inet n       -       y       -       4       smtpd
  -o syslog_name=postfix/submission
  -o smtpd_tls_security_level=encrypt
  -o smtpd_sasl_auth_enable=yes
  -o smtpd_tls_auth_only=yes
  -o smtpd_client_restrictions=permit_sasl_authenticated,reject
  -o smtpd_relay_restrictions=permit_sasl_authenticated,reject
  -o smtpd_recipient_restrictions=reject_sender_login_mismatch,permit_sasl_authenticated,reject
  -o smtpd_sender_restrictions=reject_sender_login_mismatch
  -o cleanup_service_name=cleanup

pickup    unix  n       -       y       60      1       pickup
cleanup   unix  n       -       y       -       2       cleanup
qmgr      unix  n       -       n       300     1       qmgr
tlsmgr    unix  -       -       y       1000?   1       tlsmgr
rewrite   unix  -       -       y       -       2       trivial-rewrite
bounce    unix  -       -       y       -       2       bounce
defer     unix  -       -       y       -       2       bounce
trace     unix  -       -       y       -       1       bounce
verify    unix  -       -       y       -       1       verify
flush     unix  n       -       y       1000?   1       flush
proxymap  unix  -       -       n       -       2       proxymap
proxywrite unix -       -       n       -       1       proxymap
smtp      unix  -       -       y       -       4       smtp
relay     unix  -       -       y       -       4       smtp
  -o syslog_name=postfix/$service_name

# Outbound transport for relays that use implicit TLS (port 465).
relaytls  unix  -       -       y       -       2       smtp
  -o syslog_name=postfix/relaytls
  -o smtp_tls_wrappermode=yes
  -o smtp_tls_security_level=encrypt

showq     unix  n       -       y       -       1       showq
error     unix  -       -       y       -       2       error
retry     unix  -       -       y       -       2       error
discard   unix  -       -       y       -       2       discard
local     unix  -       n       n       -       2       local
virtual   unix  -       n       n       -       2       virtual
lmtp      unix  -       -       y       -       4       lmtp
anvil     unix  -       -       y       -       1       anvil
scache    unix  -       -       y       -       1       scache
postlog   unix-dgram n  -       n       -       1       postlogd
